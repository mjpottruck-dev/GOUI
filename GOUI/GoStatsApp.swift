import SwiftUI

// Removed @main — only ONE @main App is allowed in the project.
struct GoStatsApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Disabled duplicate @main App. Use SoccerStatsMVPApp.swift as entry point.")
        }
    }
}

